#include <iostream>
#include <fstream>
#include <iomanip>

using namespace std;

int main(){
	/*long long k, n, p, m;
	double ans;
	ifstream fin("exam.in");
	ofstream fout("exam.out");
	fin >> k >> n;
	for (int i = 0; i < k; i++){
		fin >> p >> m;
		ans += p * m;
	}	
	ans = ans / (100 * n);
	fout << setprecision(5) << ans;*/
	double b;
	double c = 2.0;
	b = 3 / c;
	cout << b;
}
